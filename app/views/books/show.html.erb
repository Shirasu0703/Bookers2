<h1>showページは投稿後に移動するページ</h1>
<p>投稿した内容を編集・削除できる、投稿者の名前も出てくる</p>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Book detail</th>
    </tr>
</thead>
<tbody>
<% books.each do |book| %>
  <tr>
    <td><%= book.id %></td>
    <td><%= book.title %></td>
    <td><%= book.body %></td>
    <td><%= link_to "Edit", books_edit_path %></td>
    <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure ?" %></td>
  </tr>
<% end %>
</tbody>
</table>