<% if user_signed_in? && !(controller_name == 'homes' && action_name == 'top') %>
  <div class="col-md-8 col-lg-2 d-md-block shadow-sm rounded px-0 my-2">
  <h2>Books<h2>
    <table class="table table-striped">
      <thead>
        <tr>
          <th>Title</th>
          <th>Opinion</th>
          <th colspan = "3"></th>
        </tr>
      <thead>
      <thody>
        <% @books.each do |book| %>
          <tr>
            <td><%= book.id %></td>
            <td><%= book.title %></td>
            <td><%= book.body %></td>
          </tr>
        <% end %>
      </thody>
    </table>


        
        

   

    
  </div>
<% end %>

